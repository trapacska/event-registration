---
format_version: '13'
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git
project_type: react-native
workflows:
  ios-simulator:
    meta:
      bitrise.io:
        stack: osx-xcode-26.4.x
        machine_type_id: g2.mac.large
    description: |
      Installs dependencies.

      Next steps:
      - Add tests to your project and configure the workflow to run them.
      - Check out [Getting started with Expo apps](https://devcenter.bitrise.io/en/getting-started/getting-started-with-expo-apps.html).
    steps:
    - git-clone@8: {}
    - restore-npm-cache@3: {}
    - npm@3:
        title: npm install
        inputs:
        - command: install
    - save-npm-cache@1: {}
    - restore-cocoapods-cache@3: {}
    - script@1:
        title: Expp prebuild
        inputs:
        - content: |-
            #!/usr/bin/env bash
            # fail if any commands fails
            set -e
            # make pipelines' return status equal the last command to exit with a non-zero status, or zero if all commands exit successfully
            set -o pipefail
            # debug log
            set -x

            # write your script here
            npx expo prebuild

            # or run a script from your repository, like:
            # bash ./path/to/script.sh
            # not just bash, e.g.:
            # ruby ./path/to/script.rb
    - save-cocoapods-cache@1: {}
    - xcode-build-for-simulator@3:
        inputs:
        - configuration: release
        - xcodebuild_options: |-
            CODE_SIGN_IDENTITY=""
            CODE_SIGNING_REQUIRED=NO
            CODE_SIGNING_ALLOWED=NO
        - project_path: ios/Chess.xcworkspace
        - scheme: Chess
    - deploy-to-bitrise-io@2: {}
    triggers:
      push: []
      pull_request: []
  ios-dev-build:
    meta:
      bitrise.io:
        stack: osx-xcode-26.4.x
        machine_type_id: g2.mac.large
    status_report_name: Executing <target_id> for <project_title>
    description: |
      Installs dependencies.

      Next steps:
      - Add tests to your project and configure the workflow to run them.
      - Check out [Getting started with Expo apps](https://devcenter.bitrise.io/en/getting-started/getting-started-with-expo-apps.html).
    steps:
    - git-clone@8: {}
    - restore-npm-cache@3: {}
    - npm@3:
        title: npm install
        inputs:
        - command: install
    - save-npm-cache@1: {}
    - restore-cocoapods-cache@3: {}
    - script@1:
        title: Expo prebuild
        inputs:
        - content: |-
            #!/usr/bin/env bash
            # fail if any commands fails
            set -e
            # make pipelines' return status equal the last command to exit with a non-zero status, or zero if all commands exit successfully
            set -o pipefail
            # debug log
            set -x

            # write your script here
            npx expo prebuild

            # or run a script from your repository, like:
            # bash ./path/to/script.sh
            # not just bash, e.g.:
            # ruby ./path/to/script.rb
    - save-cocoapods-cache@1: {}
    - certificate-and-profile-installer@1: {}
    - script@1:
        title: Delete Bitrise Cert
        inputs:
        - content: |-
            #!/usr/bin/env bash
            set -e
            set -o pipefail
            set -x
            # For some reason, xcode was choosing the Bitrise wildcard cert unless it is deleted.
            sudo security delete-certificate -c 'Apple Development: Tooling Bot Bitrise'
    - xcode-archive@6:
        inputs:
        - project_path: ios/Chess.xcworkspace
        - scheme: Chess
        - configuration: Release
        - verbose_log: 'yes'
        - distribution_method: enterprise
        - automatic_code_signing: "off"
    - deploy-to-bitrise-io@2: {}
    triggers:
      push: []
app:
  envs:
  - PLATFORM: all
    opts:
      is_expand: false